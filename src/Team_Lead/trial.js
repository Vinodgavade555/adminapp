import {View, Text} from 'react-native';
import React from 'react';

const Trial = () => {
  return (
    <View
      style={{
        flex: 1,
        color: '#fff',
        fontSize: 20,
        fontWeight: 'bold',
        backgroundColor: 'red',
        // justifyContent: 'center',
      }}>
      <Text
        style={{
          flex: 1,
          color: '#fff',
          fontSize: 20,
          fontWeight: 'bold',
          backgroundColor: 'red',
          // justifyContent: 'center',
        }}>
        trial
      </Text>
    </View>
  );
};

export default Trial;
